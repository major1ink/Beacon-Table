.claude
*.exe
*.txt
/beacon-table
/beacon-table-desktop
/uploads
/data
beacon.conf
/web/node_modules
/dist
/.task

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
.mcp.json
.ignore
